{
  "server_host": {
    "description": "独立上传服务监听地址",
    "type": "string",
    "default": "127.0.0.1",
    "hint": "Cloudflare Tunnel 转发到本机时保持默认 127.0.0.1 即可；若希望局域网/公网直连此端口，可改为 0.0.0.0（注意：此服务无令牌鉴权，直连公网请自行确保安全）。"
  },
  "server_port": {
    "description": "独立上传服务端口",
    "type": "int",
    "default": 8765,
    "hint": "手机端填的根地址需与此对应（经 Cloudflare Tunnel 转发到此端口）。"
  },
  "hr_high_threshold": {
    "description": "心率过高告警阈值（次/分）",
    "type": "int",
    "default": 120,
    "hint": "单次上传批次中心率超过该值时触发告警，同设备 30 分钟内不重复告警。"
  },
  "battery_low_threshold": {
    "description": "电量过低告警阈值（%）",
    "type": "int",
    "default": 15,
    "hint": "设备上报电量低于或等于该值时触发告警。"
  },
  "alert_target_umo": {
    "description": "兜底告警推送目标（unified_msg_origin）；设备有绑定会话时告警推给所有绑定会话，仅在设备没有任何绑定会话时使用此项；留空则只入库不推送",
    "type": "string",
    "default": "",
    "hint": "形如 qq_official:botid:group:群号 或 aiocqhttp:botid:private:QQ号。多用户场景建议用 /bind 绑定设备，让告警自动推给绑定会话。"
  }
}
